events {
    worker_connections 1024;
}

http {
    upstream backend_nodes {
        server ${BACKEND_HOST}:80;
    }

    upstream frontend_node {
        server ${FRONTEND_HOST}:80;
    }

    server {
        listen 80;

        # Route API requests to the backend
        location /api/ {
            proxy_pass http://backend_nodes;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
        
        # Route health checks to the backend
        location /health {
            proxy_pass http://backend_nodes/health;
        }

        location /ready {
            proxy_pass http://backend_nodes/ready;
        }

        # Route everything else to the frontend
        location / {
            proxy_pass http://frontend_node;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
    }
}
